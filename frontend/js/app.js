/* =====================================================
   APP — Entry point, initializes shell + router
   ===================================================== */

document.addEventListener("DOMContentLoaded", () => {
  Shell.init();
  Router.init();
});
